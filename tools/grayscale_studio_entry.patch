--- a/badge_relief_maker/app/main.py
+++ b/badge_relief_maker/app/main.py
@@ -30,7 +30,7 @@ def run_gui() -> int:
         print("PySide6 is not installed. Install GUI dependencies before using --gui.")
         return 2
 
-    from .ui.editor_window import MainWindow
+    from .ui.grayscale_studio import MainWindow
 
     app = QApplication(sys.argv[:1])
     window = MainWindow()
--- a/badge_relief_maker/__init__.py
+++ b/badge_relief_maker/__init__.py
@@ -2,4 +2,4 @@
 
 """Badge Relief Maker package."""
 
-__version__ = "0.3.0"
+__version__ = "0.4.0"
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -6,8 +6,8 @@ build-backend = "setuptools.build_meta"
 
 [project]
 name = "badge-relief-maker"
-version = "0.3.0"
-description = "Local 2D badge and medal artwork to editable 2.5D relief meshes."
+version = "0.4.0"
+description = "Chinese desktop software for single-image badge and medal grayscale heightmap generation."
 readme = "README.md"
 requires-python = ">=3.10,<3.13"
 dependencies = [
